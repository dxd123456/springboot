<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zking.demo.mapper.UserMapper">

   <select id="findByName" parameterType="string" resultType="user">
     select userId,userName,password from t_user where userName=#{value}
   </select>

    <select id="fincById" parameterType="int" resultType="user">
    select  userId,userName,password,Perms from t_user where userId=#{value}
    </select>
</mapper>